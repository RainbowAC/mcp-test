# MCP工具管理服务器 - 依赖文件
# 版本锁定确保环境一致性

# 核心框架依赖
mcp>=1.0.0,<2.0.0
SQLAlchemy>=2.0.0,<3.0.0
PyMySQL>=1.0.2,<2.0.0

# 系统监控和工具
psutil>=5.8.0,<6.0.0

# 开发工具 (仅开发环境需要)
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0
black>=23.0.0,<24.0.0
flake8>=6.0.0,<7.0.0
mypy>=1.0.0,<2.0.0

# 可选依赖 (增强功能)
# python-dotenv>=1.0.0,<2.0.0  # 环境变量管理
# uvicorn>=0.20.0,<1.0.0      # ASGI服务器 (如果使用FastAPI)
# fastapi>=0.100.0,<1.0.0     # Web框架 (如果需要API)